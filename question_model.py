class Question:
    #We have attributes text and answer.
    #Passing q_text and q_answer as arguments to the init function
    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer